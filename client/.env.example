# Copy this to client/.env for local development
# (usually not needed locally since the Vite proxy handles /api)

# Set this on your Vercel FRONTEND project to point at the backend
# e.g. https://medicare-api.vercel.app
VITE_API_URL=
